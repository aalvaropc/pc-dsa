# 206. Reverse Linked List / Invertir Lista Enlazada

## 🇬🇧 English

**Problem:**  
Given the `head` of a singly linked list, **reverse** the list and return the **reversed list**.

---

### Example 1
Input: head = [1,2,3,4,5]
Output: [5,4,3,2,1]

### Example 2
Input: head = [1,2]
Output: [2,1]

### Example 3
Input: head = []
Output: []

---

### Constraints
- The number of nodes in the list is in the range `[0, 5000]`.  
- -5000 <= Node.val <= 5000  

---

**Follow-up:**  
A linked list can be reversed either **iteratively** or **recursively**.  
Could you implement both?

---

## 🇪🇸 Español

**Problema:**  
Dada la cabeza (`head`) de una lista enlazada simple, **invierte** la lista y devuelve la **lista invertida**.

---

### Ejemplo 1
Entrada: head = [1,2,3,4,5]
Salida: [5,4,3,2,1]

### Ejemplo 2
Entrada: head = [1,2]
Salida: [2,1]

### Ejemplo 3
Entrada: head = []
Salida: []

---

### Restricciones
- El número de nodos en la lista está en el rango `[0, 5000]`.  
- -5000 <= Node.val <= 5000  

---

**Pregunta adicional:**  
Una lista enlazada puede invertirse de forma **iterativa** o **recursiva**.  
¿Podrías implementar ambos métodos?